---
description: Stage and commit changes with an auto-generated message
---

1. Run git status and git diff to detect all unstaged changes
2. Present the list of changed files to the user and ask which ones to include
3. Stage only the approved files using git add [specific files]
4. Create a commit with an auto-generated descriptive message based on the staged changes
5. Display the final commit for confirmation
